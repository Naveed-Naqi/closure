import { createMuiTheme } from "@material-ui/core";

const Theme = createMuiTheme({
  palette: {
    primary: {
      main: "#003049",
    },
    secondary: {
      main: "#e3f2fd",
    },
    third: {
      main: "#212121",
    },
  },
});

export default Theme;
